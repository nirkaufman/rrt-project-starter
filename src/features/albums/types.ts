export interface Album {
    userId: number,
    id: number,
    title: string
}

export interface AlbumInfo {
    id: number;
    owner: string;
    title: string;
    thumbnail: string;
    imageCount: number;
}
